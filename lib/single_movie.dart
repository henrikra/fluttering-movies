import 'package:flutter/material.dart';

class SingleMovie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text('Hellurei')),
      body: new Text('hello there'),
    );
  }
}